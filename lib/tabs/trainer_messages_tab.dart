import 'package:flutter/material.dart';

class TrainerMessagesTab extends StatelessWidget {
  const TrainerMessagesTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Messages'),
    );
  }
}
